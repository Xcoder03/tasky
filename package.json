{

  "name": "tasky",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "type": "module",

  "keywords": [
    "nodejs",
    "mongoose"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon, app.js"
  },

  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "init": "^0.1.2",
    "mongoose": "^7.0.3",
    "nodemon": "^2.0.22"
  }
}
